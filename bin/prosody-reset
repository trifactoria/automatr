#!/usr/bin/env bash
set -euo pipefail

NAME="automatr-prosody"
VOL="automatr-prosody-data"

docker rm -f "$NAME" >/dev/null 2>&1 || true
docker volume rm "$VOL" >/dev/null 2>&1 || true
echo "Reset: removed $NAME and volume $VOL"
